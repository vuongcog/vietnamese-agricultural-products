import { ADMIN_LAYOUT } from "../../constants/langs-scope";

const langs = {
  user: ADMIN_LAYOUT + ".user",
  category: ADMIN_LAYOUT + ".category",
  product: ADMIN_LAYOUT + ".product",
  blog: ADMIN_LAYOUT + ".blog",
  blogCategory: ADMIN_LAYOUT + ".blogCategory",
  coupon: ADMIN_LAYOUT + ".coupon",
  banner: ADMIN_LAYOUT + ".banner",
};
export default langs;
