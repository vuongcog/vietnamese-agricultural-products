import { useSelector } from 'react-redux';
import { getIsOrdering } from '../../selectors/user/selectors-order';

const useProducerOrder = () => {
  const isOrdering = useSelector(getIsOrdering);
  return { isOrdering };
};
export default useProducerOrder;
