import { USER_ORDER } from '../../../constants/langs-scope';

const langs = {
  firstName: USER_ORDER + '.firstName',
  lastName: USER_ORDER + '.lastName',
  email: USER_ORDER + '.email',
  phone: USER_ORDER + '.phone',
  address: USER_ORDER + '.address',
  notes: USER_ORDER + '.notes',
  province: USER_ORDER + '.province',
  city: USER_ORDER + '.city',
  district: USER_ORDER + '.district',
  ward: USER_ORDER + '.ward',
  township: USER_ORDER + '.township',
  houseNumber: USER_ORDER + '.houseNumber',
  street: USER_ORDER + '.street',
  payment: USER_ORDER + '.payment',
  totalPrices: USER_ORDER + '.totalPrices',
  order: USER_ORDER + '.order',
};
export default langs;
