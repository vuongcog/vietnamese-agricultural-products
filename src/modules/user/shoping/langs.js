import { USER_SHOPPING } from "../../../constants/langs-scope";

const langs = {
  filter: USER_SHOPPING + ".filter",
  category: USER_SHOPPING + ".category",
  priceRange: USER_SHOPPING + ".priceRange",
  search: USER_SHOPPING + ".search",
  from: USER_SHOPPING + ".from",
  to: USER_SHOPPING + ".to",
};
export default langs;
